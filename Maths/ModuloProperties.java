package Maths;

/*
* (a + b) % m = ((a % m) + (b % m)) % m
* (a - b) % m = ((a % m) - (b % m) + m) % m
* (a * b) % m = ((a % m) * (b % m)) % m
* (a / b) % m = ((a % m) * ((1 / b) % m)) % m
* bpow-1 % m => Multiplicative modulo inverse(mmi)
* mmi => bpow-1 % m means that b and m are co-primes
* (a % m) % m = a % m
* m % m = 0 forall x belongs to +ve integers
 */

public class ModuloProperties {
}
